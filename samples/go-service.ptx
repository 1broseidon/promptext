code:
  config_yaml: |
    server:
      port: 8080
      host: localhost

  go_mod: |
    module example.com/go-service

    go 1.22.0

  main_go: |
    package main

    import (
    	"fmt"
    	"net/http"
    )

    func main() {
    	http.HandleFunc("/", func(w http.ResponseWriter, r *http.Request) {
    		fmt.Fprintf(w, "Hello from Go!")
    	})
    	http.ListenAndServe(":8080", nil)
    }

files[3]{ext,lines,path}:
  yaml,4,config.yaml
  mod,4,go.mod
  go,14,main.go
metadata:
  language: Go
  total_files: 3
  total_lines: 22
  version: 1.22.0
stats:
  packages: 0
  totalFiles: 3
  totalLines: 22
structure:
  [3]: config.yaml,go.mod,main.go