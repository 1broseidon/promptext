code:
  main_py: |
    from flask import Flask

    app = Flask(__name__)

    @app.route('/')
    def hello():
        return {'message': 'Hello World'}

    if __name__ == '__main__':
        app.run(debug=True)

  pyproject_toml: |
    [tool.poetry]
    name = "python-api"
    version = "0.1.0"
    description = ""
    authors = ["Your Name <you@example.com>"]
    readme = "README.md"

    [tool.poetry.dependencies]
    python = "^3.12"


    [build-system]
    requires = ["poetry-core"]
    build-backend = "poetry.core.masonry.api"

  requirements_txt: |
    Flask==2.0.1
    python-dotenv==0.19.0
    pytest==7.0.1

  setup_py: |
    from setuptools import setup

    setup(
        name='python-api-sample',
        version='1.0.0',
        description='Sample Python API',
        packages=[''],
    )

  test_main_py: |
    def test_hello():
        assert True

files[5]{ext,lines,path}:
  py,11,main.py
  toml,15,pyproject.toml
  txt,4,requirements.txt
  py,9,setup.py
  py,3,test_main.py
metadata:
  language: Python
  total_files: 5
  total_lines: 42
  version: "3.12"
stats:
  packages: 0
  totalFiles: 5
  totalLines: 42
structure:
  [5]: main.py,pyproject.toml,requirements.txt,setup.py,test_main.py